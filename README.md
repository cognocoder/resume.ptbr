A single-page, one-column resume for software developers in LaTex.

### Build

```sh
make          # Builds the resume. 
make run      # Run the resume using okular as pdf viewer.
make clean    # Remove non source files.
```

